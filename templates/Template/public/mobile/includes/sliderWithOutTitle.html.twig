<section class="container mb-1">
    <h2 class="fs-5 mb-3">
        {{section_name}}
    </h2>
    <div class="d-flex gap-2 overflow-auto invisibility-scroll">
        {% for i in 1..10 %}
           
            {% set random_item = random(1, 5) %}
                <div class="col-5">
                    <div class="card border-0" >
                        <img class="card-img-top rounded-4 mb-1" style="height: 130px ;" src="/assets/comfort/img/{{random_item}}.jpg" alt="Картинка категории {{ i }}">
                    </div>
                </div>
        {% endfor %}
    </div>
</section>

<hr class="mt-0 mb-2">
