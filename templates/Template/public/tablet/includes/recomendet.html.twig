<section class="container ">
   <div class="d-flex align-items-center gap-3 mb-3">
            <h2 class="fs-5 m-0">Рекомендуем</h2>
            <button class="btn p-0" style="font-size: 14px;">
              <u>Скидки</u>
            </button>
            <button class="btn p-0" style="font-size: 14px;">
              <u>Новинки</u>
            </button>
            <button class="btn p-0" style="font-size: 14px;">
              <u>Коллекции</u>
            </button>
          </div>

    <div class="d-flex gap-1 overflow-auto invisibility-scroll">
        {% for i in 0..10 %}
           {% set section_number = 50000%}
            <div class="col-3">{% include Template('@comfort:Template/'~baks_settings.device~'/includes/card.html.twig') %}</div>
               
        {% endfor %}
     </div>   
</section>
<hr class="mt-0">